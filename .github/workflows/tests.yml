name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tests:
    uses: liberusoftware/.github/.github/workflows/package-tests.yml@main
    with:
      phpstan-level: 10
      coverage-threshold: 95
